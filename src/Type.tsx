export interface wrapperType {
  index: number;
}

export interface propsType {
  MonthlyPrice: boolean;
}

export interface priceContainerType {
  MonthlyPrice: boolean;
  handleClick: () => void;
}
